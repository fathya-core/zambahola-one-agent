# Mean reversion

**Source concepts:** Pring — oscillators around mean; Chan — pairs and reversion.

**Logic:** SMA over recent ticks; price far above SMA → expect `down`, far below → `up`.

**Improve accuracy:** Use longer SMA in trending markets; combine with regime filter.
