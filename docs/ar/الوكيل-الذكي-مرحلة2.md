# الوكيل الذكي — المرحلة 2 (محلل + متنبئ أقوى)

**بدون backtest** — التقييم حي + تجارب عتبات داخلية فقط.

## ما أُضيف

| الميزة | الوظيفة |
|--------|---------|
| **Micro gates** | spread>5bp · vol عالي · imbalance · prob≥0.58 |
| **Meta-PnL (Model 2)** | يتعلم من صفقات paper: هل الدخول كان مربحاً؟ |
| **المحلل العربي** | يشرح سبب الامتناع/الإشارة في اللوحة |
| **Pattern journal v2** | إحصائيات حسب `gateReason` |
| **Calibration API** | منحنى reliability في اللوحة |
| **Order book متعدد المستويات** | imbalance5 + imbalance20 |
| **Experiments 01–05** | sweep عتبات (replay داخلي — ليس backtest للمستخدم) |

## التشغيل (PowerShell)

```powershell
cd C:\Users\pc\zambahola-one-agent
git pull origin main
npm run agent:stop
.\scripts\restart-directional.ps1
```

أو يدوياً:

```powershell
npm run agent:research-import -- apps/one-agent/knowledge/user-reports/AGENT-IMPORT-FINAL.json
npm run agent:phase2-live
```

## تجارب العتبات (اختياري — على جهازك)

```powershell
npm run agent:experiments:quick    # تجربة 01 فقط (~5 دقائق)
npm run agent:experiments          # 01–05 كاملة
```

النتائج: `apps/one-agent/data/learning/experiments/`

## متغيرات Phase 2 (`config/phase2-live.env`)

| Env | القيمة | المعنى |
|-----|--------|--------|
| `ZAMBAHOLA_LABEL_BP` | 2.5 | منطقة flat للتقييم |
| `ZAMBAHOLA_MAX_SPREAD_BP` | 5 | امتناع إذا السبريد أعلى |
| `ZAMBAHOLA_MIN_MAIN_PROB` | 0.58 | عتبة النموذج الأساسي |
| `ZAMBAHOLA_META_THRESHOLD` | 0.55 | meta-label |
| `ZAMBAHOLA_META_PNL_THRESHOLD` | 0.55 | meta-PnL |
| `ZAMBAHOLA_ANALYST_AR` | 1 | شرح عربي |

## ماذا تراقب؟

| المقياس | الهدف |
|---------|--------|
| Directional hit | ≥ 58–62% |
| Abstain | 40–75% (ليس 95%+) |
| Analyst | يظهر سبب واضح بالعربي |
| Calibration score | يتحسن مع الوقت |

## أوامر مفيدة

```powershell
npm run agent:patterns
curl http://127.0.0.1:8787/api/analyst
curl http://127.0.0.1:8787/api/calibration
```
