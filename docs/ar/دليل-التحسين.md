# دليل ZAMBAHOLA — النسخة الذكية v0.2

## v0.5 MEGA — الأقوى

| ميزة | وصف |
|------|-----|
| **hybrid_v5_mega** | 5 طبقات ذكاء (ensemble + ML + MLP + GBM + LOB-CNN) |
| **11 استراتيجية** | + long/short extreme |
| **1200+ شمعة** | تاريخ paginated |
| **mega-train** | تدريب دفعي على التاريخ |
| **mega-backtest** | اختبار 1200 شمعة |

```bash
npm run agent:mega-train
npm run agent:mega-backtest
npm run agent:start
```

---

## v0.4 DEEP — أعمق محرك

| ميزة | وصف |
|------|-----|
| **hybrid_v4_deep** | Ensemble + ML خطي + **شبكة MLP** |
| **18 ميزة** | funding، premium، long/short، volume، وقت |
| **10 استراتيجيات** | + funding fade + volume breakout |
| **إشارات السوق** | Binance FAPI بدون مفاتيح |
| **عمق مزدوج** | Binance + Bybit order book |
| **deep-backtest** | 500 شمعة |
| **deep-learn** | 15 دورة + backtest |

```bash
npm run agent:deep-backtest
npm run agent:deep-learn
npm run agent:start
```

بحث: `docs/DEEP_RESEARCH_2026.md`

---

## v0.3 — أقصى قوة

| ميزة | وصف |
|------|-----|
| **Universal feed** | Binance + عمق السوق + Bybit احتياطي |
| **8 استراتيجيات** | + MACD + عدم توازن دفتر الأوامر |
| **13 ميزة ML** | بما فيها السيولة والسبريد |
| **backtest** | `npm run agent:backtest` |
| **power-learn** | `npm run agent:power-learn` — 10 دورات |

```bash
npm run agent:start
npm run agent:power-learn
npm run agent:backtest
```

افتراضي: `ZAMBAHOLA_FEED=universal`

---

## ما الجديد (v0.2)

| القدرة | الوصف |
|--------|--------|
| **Binance مباشر** | بث `btcusdt` حي (افتراضي) — `ZAMBAHOLA_FEED=mock` للتجارب |
| **Hybrid v2** | Ensemble + تعلم آلي online + بوابة نظام السوق |
| **مشاعر** | Fear & Greed + عناوين CoinDesk RSS |
| **معايرة الثقة** | تعديل confidence حسب النتائج الفعلية |
| **تعلم مستمر** | أوزان استراتيجيات + ML weights + سجل بحث |

## التشغيل

```bash
npm run setup
npm run agent:start          # Binance افتراضي
ZAMBAHOLA_FEED=mock npm run agent:start   # محاكاة
npm run agent:learn          # 5 دورات تعلم
npm run agent:turbo-learn    # تعلم سريع mock
npm run agent:stop
```

## متغيرات البيئة

| المتغير | القيم | المعنى |
|---------|-------|--------|
| `ZAMBAHOLA_FEED` | `binance` / `mock` | مصدر السعر |
| `ZAMBAHOLA_LEARN_CYCLES` | رقم | دورات `agent:learn` |

## واجهة API

- `/api/sentiment` — المشاعر الحالية
- `/api/strategies` — أوزان + إحصائيات
- `/api/knowledge` — الفهرس الكامل

## التوقف

```bash
npm run agent:stop
```

أو قل **توقف** وسيتوقف التطوير عند الطلب.
