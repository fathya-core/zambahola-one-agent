# مراجعة BUNDLE-FOR-REVIEW.md — كاملة

**التاريخ:** 2026-06-06 | **الملفات:** 5 | **السطور:** ~2125

---

## ملخص كل تقرير

| التقرير | المحتوى | مفيد للوكيل الآن؟ |
|---------|---------|-------------------|
| **دقة التنبوء ورفعه** | meta-labeling، labels 2–3bp، orderbook، XGBoost، abstention | **نعم** — يؤكد مسار الوكيل |
| **تعلم الآلة والذكاء الاصطناعي** | pipeline كامل، features، walk-forward، agent loop | **نعم** — خريطة طريق |
| **ايه.md** | هياكل Python (backtest, meta, ensemble) | **مرجع كود** — ليس أوزان |
| **قيود يدوية** | نفس الهياكل بدون قيود | **مرجع** — قيودك في config |
| **تنبوء-2** | experiments 01–05، calibration، thresholds | **نعم** — تجارب منطقية |

**ملاحظة:** المعاينة التلقائية كانت `entries: []` لأن التقارير **نص + Python** بدون JSON أوزان — لذلك بنينا `AGENT-IMPORT-FINAL.json` يدوياً.

---

## ما يطابق الوكيل الحالي (جاهز)

| توصية التقارير | في ZAMBAHOLA |
|----------------|--------------|
| Ensemble (XGB+LSTM+Logit) | ml + mlp + gbm + 17 strategy |
| Meta-labeling + abstention | max-accuracy + expert-consensus |
| منع mean_reversion في ترند | expert-consensus |
| order_imbalance / microstructure | order_imbalance, premium, funding, OI |
| Walk-forward | walk-forward-trainer + omni-train |
| أفق 30–60 ثانية | horizon 45 ثانية |
| directional hit أهم من accuracy خام | hit guard اتجاهي |

---

## أوزان نطبّقها (FINAL)

| استراتيجية | السبب من الحزمة |
|------------|-----------------|
| order_imbalance **×1.4+** | أقوى feature في أبحاث orderbook |
| momentum, tick_momentum **↑** | returns/z-score في الترند |
| ema_cross, macd, atr **↑** | trend + technicals |
| premium, OI, volume **↑** | microstructure Binance |
| volatility_regime **↑** | abstention عند vol عالي |
| mean_reversion, rsi, bollinger **↓** | حظر/تخفيض في ترند |
| funding_fade **↓** | ترند قوي + funding عالي |

**هدف:** directional hit **62%**

---

## ما لم يُنفَّذ بعد (v0.5+)

- سكربتات `experiments/01–05` كملفات Python منفصلة
- Meta-model ثانٍ مستقل (Model2 على PnL)
- spread_bp > 5 → abstain صريح
- Label threshold sweep 2–3 bp كـ env قابل للضبط
- LSTM/CNN كامل على تسلسل طويل

---

## أوامر الوكيل (بعد هذه المراجعة)

```powershell
git pull origin main
npm run agent:research-import -- apps/one-agent/knowledge/user-reports/AGENT-IMPORT-FINAL.json
npm run agent:omni-train
npm run agent:max-accuracy:start
```

راقب: **Directional hit** ≥ 62% قبل Binance demo.
